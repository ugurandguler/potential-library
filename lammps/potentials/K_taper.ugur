# K - Ugur interatomic potential, cutoff switched off over its outer 15 %
#
# pair_style ugur
# pair_coeff * * K_taper.ugur K
#
# Truncation: switched from 0.85 of each cutoff to the cutoff, quintic, C2 - energy is conserved in MD
# MD: screened - holds its structure at 600 K (313 K, as equipartition requires).
#
# Nudge test: passed - displaced by 1e-5 A and relaxed, the lattice returns.
#
# Fitted to: cohesive energy, lattice constant, bulk modulus and the elastic
# constants of K at its experimental lattice constant.  Nothing else.
#
# Format is LAMMPS's Tersoff convention - one line per ordered triple
# (centre, leg, leg) - so that this file and a multi-element one are read the
# same way.  For a single element that is one line.
#
# el1 el2 el3  m D alpha r0 gamma C alpha3 rcut2 rcut3 taper lam2 lam4
K K K 1.2000131178354225 0.083033010532054349 0.7123706485449095 5.7285167021743755 3.1751956843074634e-08 -0.16560583460182737 0.71661343464259619 13.852800000000002 6.9212750270452332 0.84999999999999998 0 0
