# Cu - Ugur interatomic potential, hard truncation, with the angular factor
#
# pair_style ugur/ang
# pair_coeff * * Cu.ugur.ang Cu
#
# Truncation: hard - phi2 does not vanish at rcut2, it stops at -2.94 meV (0.115 of k_B T at 296 K).  USE for static properties and for lattice dynamics: measured against the measured dispersion the hard sets average 9.7 % over the 33 elements that have one, against 12.6 % for the switched sets, and are closer in 25 of them.  DO NOT use for molecular dynamics: copper, whose step is 8.4 meV, drifts 350 meV/atom/ns in NVE and climbs from 296 K to over 1100 K in 200 ps
# MD: structure screened - holds its shape at 600 K (308 K, as equipartition requires).  This tested the SHAPE, not energy conservation; for that see the truncation line above.
#
# Lattice stability: screened on the 8^3 and 9^3 meshes AND along the
# symmetry path; no imaginary modes.
#
# Nudge test: passed - displaced by 1e-5 A and relaxed, the lattice returns.
#
# Fitted to: cohesive energy, lattice constant, bulk modulus and the elastic
# constants of Cu at its experimental lattice constant.  Nothing else.
#
# Format is LAMMPS's Tersoff convention - one line per ordered triple
# (centre, leg, leg) - so that this file and a multi-element one are read the
# same way.  For a single element that is one line.
#
# el1 el2 el3  m D alpha r0 gamma C alpha3 rcut2 rcut3 taper lam2 lam4
Cu Cu Cu 8.2631089394973714 0.36814339800538631 0.48250048645900445 2.6960069838014467 1.3807873888708069 -1.4770359696082287 0.95740955399143357 9.3990000000000009 3.8342865209840538 -1 -0.88050754604316683 0.96351620331824217
