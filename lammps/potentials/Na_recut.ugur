# Na - Ugur interatomic potential, CANDIDATE, re-cut, switched
#
# pair_style ugur
# pair_coeff * * Na_recut.ugur Na
#
# Truncation: switched from 0.85 of each cutoff to the cutoff, quintic, C2 - energy is conserved in MD.  CANDIDATE, not the recommended set: thermal expansion -39e-6/K, negative; C11 stiffens from 19 K to 300 K (x2.618); 300 K elastic error 93.1 % against 18.1 % for the switched set.  For MD on this element use Na_taper.ugur
# MD: structure screened - holds its shape at 600 K (330 K, as equipartition requires).  This tested the SHAPE, not energy conservation; for that see the truncation line above.
#
# Lattice stability: screened on the 8^3 and 9^3 meshes AND along the
# symmetry path; no imaginary modes.
#
# Nudge test: passed - displaced by 1e-5 A and relaxed, the lattice returns.
#
# Measured dispersion: 5.8 % against 16.2 % for the published switched
# arm - better.
# Read that beside the finite-temperature note below rather than instead of
# it.  Both are the same fact: a dispersion at 0 K is a property of the
# curvature AT the minimum, and this arm has that neighbourhood right
# while the shape of the well away from it is wrong.
#
# FINITE TEMPERATURE: this candidate passes the screens above and fails
# below room temperature - thermal expansion comes out NEGATIVE (-39e-6/K against a measured 71); C11 RISES 213 % between 0 K and 223 K instead of softening.
# Use it for static properties only, if at all.
#
# Fitted to: cohesive energy, lattice constant, bulk modulus and the elastic
# constants of Na at its experimental lattice constant.  Nothing else.
#
# Format is LAMMPS's Tersoff convention - one line per ordered triple
# (centre, leg, leg) - so that this file and a multi-element one are read the
# same way.  For a single element that is one line.
#
# el1 el2 el3  m D alpha r0 gamma C alpha3 rcut2 rcut3 taper lam2 lam4
Na Na Na 7.4653863186764315 0.24051541689262698 0.32240662762157185 3.6440359967543468 0.68040759785429805 -0.20813275614441329 0.47311392196599661 5.1797814248836529 5.5736528962162692 0.84999999999999998 0 0
